# Frontend (React + Vite)

Quick starter for the clustering UI.

Dev

```bash
cd frontend
npm install
npm run dev
```

The Vite dev server is configured to proxy `/api` to the backend when you set it up (use CORS or a proxy). Build with `npm run build`.
